# 飞书长记忆代理项目开发日志

---

【开发日志】
时间：2026-04-25 19:57:36
本次新增：
1. 项目初始化，创建基础目录结构
2. 提交初始项目文件，完成git仓库初始化

本次修改：
暂无

测试结果：
✅ 项目骨架创建完成

问题与后续计划：
1. 后续搭建核心框架和主要模块
2. 完善项目基础配置

---

【开发日志】
时间：2026-04-25 23:07:15
本次新增：
1. 完成项目核心架构搭建
2. 实现FastAPI后端服务基础框架
3. 实现CLI命令基础框架（mem命令雏形）
4. 完成数据模型设计（关系型+向量数据库）
5. 完成飞书机器人基础接口开发
6. 生成初步开发文档、环境配置指南等设计文档

本次修改：
1. 配置pyproject.toml项目元数据和依赖
2. 配置.gitignore等项目配置文件

测试结果：
✅ 项目框架可以正常启动
✅ 基础CLI命令可以正常运行
✅ 后端服务接口可以正常访问

问题与后续计划：
1. 待完善CLI端功能增强
2. 待开发飞书机器人完整对接逻辑
3. 待实现记忆引擎核心功能

---

【开发日志】
时间：2026-04-26 23:05:33
本次新增：
暂无

本次修改：
1. 降级click和typer版本，解决版本兼容问题
2. 修复pyproject.toml配置错误
3. 修正飞书SDK包名和版本号

测试结果：
✅ 依赖安装无报错
✅ 版本冲突问题初步解决

问题与后续计划：
1. 需进一步验证mem命令全局运行情况
2. 继续完善方向A相关功能开发

---

【开发日志】
时间：2026-04-26 23:50
本次新增：
1. 新增`开发文档/cli/`目录，包含5个分阶段开发文档
   - 0-前置准备.md：环境依赖配置说明
   - 1-后端API接口开发.md：CLI相关接口实现指南
   - 2-CLI功能增强开发.md：三大CLI功能实现
   - 3-核心引擎优化.md：搜索相关性算法优化
   - 4-集成测试与验收.md：全流程测试与上线检查清单
2. 新增方向A功能开发详细指南，包含完整的代码实现和测试用例
3. 解决typer/click版本冲突问题，全局`mem`命令恢复正常运行

本次修改：
1. 更新`pyproject.toml`依赖版本，typer固定为0.12.3，click固定为8.1.7
2. 优化方向A开发文档结构，增加分阶段测试指南

测试结果：
1. ✅ `mem --help`命令正常输出所有可用命令
2. ✅ 依赖降级验证通过，版本冲突问题彻底解决
3. ✅ 所有开发文档生成完成，结构清晰可直接使用

问题与后续计划：
1. 待开发：CLI端三大功能（交互式搜索、自动命令捕获、工作流管理）
2. 待开发：CLI相关后端API接口
3. 待优化：搜索相关性算法适配CLI场景
4. 后续将按分阶段文档逐步推进功能开发

---

【开发日志】
时间：2026-04-27 00:30
本次新增：
1. 完成CLI后端两个核心接口开发
   - `/api/v1/cli/command/record`：命令记录接口，支持上报和去重存储
   - `/api/v1/cli/command/suggest`：命令推荐接口，支持按关键词搜索和频率排序
2. 新增临时内存存储模块，用于接口功能测试，可无缝对接后续核心存储

本次修改：
1. 重构`backend/routers/cli.py`，替换原有空接口为完整实现
2. 修复`backend/routers/__init__.py`导入错误，移除不存在的模块引用
3. 简化`backend/main.py`路由注册，暂时移除未实现的memory和feishu路由
4. 更新`pyproject.toml`，将typer版本固定为0.12.3，新增pyperclip依赖

测试结果：
✅ 后端服务启动成功，运行在http://127.0.0.1:8000
✅ 命令记录接口测试通过，返回正确响应
✅ 命令推荐接口测试通过，正确返回匹配的命令列表
✅ 接口符合设计规范，可直接对接CLI端开发

问题与后续计划：
1. 当前使用临时内存存储，后续需要集成核心存储引擎
2. 下一步进入阶段2开发：CLI端三大功能增强（交互式搜索、自动命令捕获、工作流管理）
3. 待解决完整依赖安装问题（pandas编译错误）

---

【开发日志】
时间：2026-04-27 13:30
本次新增：
1. 完成阶段2：CLI功能增强开发
   - `mem search`支持`--limit`、`--execute`、`--copy`、`--type`参数
   - `mem search`支持交互式选择搜索结果并执行或复制命令
   - 新增`mem watch`命令，支持扫描powershell/bash/zsh历史并记录高频命令
   - 新增`mem workflow save/run`命令，支持多步骤工作流保存和逐步确认执行
2. 新增CLI请求兼容层，优先使用阶段文档中的接口，失败时兼容现有`/memory/retrieve`和`/memory/extract`

本次修改：
1. 更新`cli/main.py`，集中实现阶段2三大CLI能力
2. 更新`requirements.txt`，补充`pyperclip`依赖
3. 修复重复命令记录时`metadata.count`未正确累加的问题

测试结果：
✅ `pytest tests\unit\test_cli_phase2.py -q -p no:cacheprovider`通过
✅ CLI帮助信息可正常展示`watch`和`workflow`命令

问题与后续计划：
1. 阶段2功能已具备可测试闭环
2. 下一步进入阶段3：核心引擎优化，增强CLI命令搜索排序相关性

---

【开发日志】
时间：2026-04-27 14:30
本次新增：
1. 按git提交历史补齐单元测试覆盖
   - CLI基础命令与配置读取测试
   - CLI后端API命令记录与推荐测试
   - 核心存储、检索、向量客户端、embedding包装测试
   - memory/feishu路由隔离测试
2. 新增`backend/schemas/memory.py`和`backend/schemas/__init__.py`，补齐memory路由依赖的Pydantic schema

本次修改：
1. 避免在测试中使用与当前`httpx/starlette`版本不兼容的`TestClient`
2. 后端API单测改为直接调用路由函数，保持unit test稳定且不依赖ASGI测试客户端版本组合

测试结果：
✅ `pytest tests\unit -q -p no:cacheprovider`通过
✅ 当前单元测试总数：43 passed

问题与后续计划：
1. 仍存在SQLAlchemy与`datetime.utcnow()`弃用警告，后续可单独处理
2. 继续推进阶段3核心引擎优化

---

【开发日志】
时间：2026-04-27 15:10
本次新增：
1. 完成阶段3：核心引擎优化开发
2. 新增CLI命令搜索相关性评分逻辑
   - 基础语义相似度得分
   - 高频使用加权，最多加0.3分
   - 最近使用加权，最多加0.2分
   - 命令前缀匹配加权
3. 新增CLI命令排序硬规则：完全前缀匹配优先于普通语义匹配
4. 向量检索结果增加metadata回传，用于CLI命令的使用次数和最近使用时间排序

本次修改：
1. 更新`core/retriever.py`，增加`calculate_cli_relevance_score`、前缀匹配判断和CLI专用排序逻辑
2. 更新`db/vector/client.py`，在搜索结果中保留Chroma返回的metadata
3. 更新核心组件单元测试，覆盖前缀优先、高频优先、最近使用优先和metadata回传

测试结果：
✅ `pytest tests\unit -q -p no:cacheprovider`通过
✅ 当前单元测试总数：48 passed

问题与后续计划：
1. 阶段3排序规则已按开发文档完成
2. 后续可进入阶段4：集成测试与验收

---

【开发日志】
时间：2026-04-27 15:13:10
关联提交：`2d3feeaf6052da076d2aadfe0d99ffe5ba748d51`

本次新增：
1. 完成CLI与memory相关数据的真实数据库持久化改造
2. 在`memories`表中新增字段：
   - `description`：保存记忆描述、命令描述或工作流说明
   - `memory_metadata`：以JSON文本保存使用次数、shell、目录、工作流步骤等扩展信息
3. 新增数据库模式初始化与轻量迁移能力：
   - `backend.dependencies.init_database_schema()`负责创建表并补齐新增字段
   - 后端启动时通过FastAPI startup事件自动初始化数据库
   - `python init_db.py`也会调用同一套初始化逻辑
4. 新增`/api/v1/cli/command/list`接口，从数据库读取已记录CLI命令

本次修改：
1. `backend/routers/memory.py`从临时数组为主改为真实数据库为主
   - `/memory/extract`、`/memory/`写入SQLite关系库
   - `/memory/list`、`/memory/search`、`/memory/retrieve`从数据库读取
   - 搜索时优先尝试向量检索，失败或无结果时使用数据库关键词与中文意图匹配兜底
2. `backend/routers/cli.py`从临时数组为主改为真实数据库为主
   - `/cli/command/record`将命令写入`memories`表，类型为`cli_command`
   - 重复命令会累加`memory_metadata.count`并更新`last_used_at`
   - `/cli/command/suggest`基于数据库中的`cli_command`进行推荐
3. `core/storage.py`将关系库作为主存储，向量库作为增强索引
   - 记忆先写入SQLite
   - embedding或ChromaDB不可用时不影响关系库保存
   - 向量客户端与embedding逻辑改为懒加载，避免缺少`chromadb`时保存接口直接500
4. `cli/main.py`增强搜索和列表读取链路
   - `/memory/search`为空时继续尝试`/memory/retrieve`
   - memory无结果时兜底调用`/cli/command/suggest`
   - `mem list`合并展示普通记忆和CLI命令记录
5. `db/relational/models.py`同步新增数据库字段，`init_db.py`改为使用统一初始化函数

测试结果：
✅ `pytest tests\integration tests\unit -q -p no:cacheprovider`通过
✅ 当前测试总数：56 passed
✅ 已验证本地`db/memory.db`表结构包含`description`和`memory_metadata`字段

问题与后续计划：
1. CLI与memory核心数据已基于真实SQLite数据库持久化
2. 临时数组仅保留为单元测试无数据库Session时的fallback
3. 后续可继续清理FastAPI `on_event`、SQLAlchemy `declarative_base()`、`datetime.utcnow()`等弃用警告

---

【开发日志】
时间：2026-04-27 15:30

本次新增：
1. 新增CLI阶段4集成验收测试文件：
   - `tests/integration/test_cli_acceptance_flow.py`
2. 集成测试覆盖完整CLI验收链路：
   - `mem watch`扫描shell历史并记录高频命令
   - `mem search "docker exited"`搜索自动记录命令
   - `mem search "docker exited" --copy`验证复制链路
   - `mem workflow save`保存中文工作流
   - `mem workflow run`逐步确认执行并支持跳过
   - `mem list`合并展示memory记忆和CLI命令记录
3. 单元测试补充覆盖：
   - `/cli/command/suggest`支持非连续多关键词匹配，例如`docker exited`
   - `/cli/command/list`返回最近记录命令
   - CLI搜索在`/memory/search`为空时继续尝试`/memory/retrieve`
   - memory搜索支持中文意图词与命令关键词映射，例如`启动webapp容器`

本次修改：
1. 更新`开发文档/cli/4-集成测试与验收.md`
   - 追加本轮验收结果
   - 标记上线前检查清单完成状态
   - 记录已覆盖的完整CLI流程
2. 更新`README.md`
   - 修复快速开始中`aem`命令笔误为`mem`
3. 更新`requirements.txt`
   - 将飞书依赖从`feishu-python-sdk`对齐为`lark-oapi==1.5.5`
   - 将`typer`版本对齐为`0.12.3`
4. 调整`init_db.py`相关单测，改为验证统一数据库初始化入口`init_database_schema()`

测试结果：
✅ `pytest tests\integration tests\unit -q -p no:cacheprovider`通过
✅ 当前测试总数：56 passed

问题与后续计划：
1. 当前未提交内容主要是验收测试、文档和依赖版本对齐
2. 后续提交时建议与数据库持久化代码分开提交，保持提交主题清晰

---

【开发日志】时间：2026-04-27 15:22

本次新增：
1. 新增 CLI 方向参赛测试数据集：
   - `test/data/cli_effectiveness_dataset.json`
2. 测试集覆盖三类验收场景：
   - 抗干扰测试：一周前注入 6 条关键 CLI 记忆，后续加入 360 条干扰数据，信噪比为 `1:60`
   - 矛盾更新测试：3 组先旧后新的冲突指令，验证最新记忆覆盖旧记忆
   - 效能指标验证：10 个高频 CLI 任务，统计字符数、操作步数和耗时变化
3. 在 `开发文档/cli/4-集成测试与验收.md` 中追加参赛测试集验收记录和量化数据。

本次修改：
1. 补充 CLI 方向比赛验收所需的量化测试数据
2. 将测试数据指标写入文档，便于后续答辩、演示和自动化测试复用

测试结果：
1. `pytest tests\unit tests\integration -q -p no:cacheprovider` 通过
2. 当前测试结果：`56 passed, 4 warnings in 1.75s`
3. 测试数据集 JSON 解析通过，关键指标复算通过：
   - 抗干扰测试：关键记忆 6 条，干扰数据 360 条，信噪比 `1:60`
   - 预期 `Hit@1 = 100.0%`，`Hit@3 = 100.0%`
   - 矛盾更新测试：3 组冲突用例，预期最新记忆胜出率 `100.0%`
   - 效能指标：平均字符输入减少 `80.3%`，平均操作步数减少 `58.0%`，平均耗时减少 `78.2%`

问题与后续计划：
1. 当前测试集已经满足比赛要求的抗干扰、矛盾更新和效能指标验证
2. 后续可将该 JSON 数据集接入 pytest，形成可自动执行的真实检索命中率评测脚本

---

【开发日志】时间：2026-04-28 10:20

本次新增：
1. 按 `开发文档/cli/5-优化方向分析.md` 推进第一批 CLI 优化
2. 新增命令参数模式抽取模块：
   - `core/command_parser.py`
3. 新增 CLI 前缀推荐命令：
   - `mem suggest <prefix>`

本次修改：
1. `cli/main.py`
   - 新增 `_current_directory()`，用于采集当前项目目录上下文
   - `mem search` 调用 `/memory/search` 和 `/cli/command/suggest` 时携带 `directory`
   - `mem watch` 自动记录高频命令时携带当前目录
   - `mem workflow run` 搜索工作流时携带当前目录
   - 新增 `mem suggest`，输出纯命令候选，便于后续接入 shell completion
2. `backend/routers/cli.py`
   - `record_command()` 保存 `directory` 和 `directories` 目录使用统计
   - `record_command()` 保存 `command_pattern`，记录 program、subcommand、flags、positionals、paths
   - `suggest_command()` 增加目录上下文排序，同目录命令优先
   - `suggest_command()` 支持基于命令模式文本匹配候选
3. `backend/routers/memory.py`
   - `/memory/search` 新增 `directory` 参数
   - 搜索排序中加入目录相关性加权
4. 测试同步更新
   - 增加目录上下文推荐测试
   - 增加命令解析测试
   - 增加 `mem suggest` 输出测试

测试结果：
1. 针对性测试通过：
   - `pytest tests\unit\test_backend_cli_api.py tests\unit\test_cli_phase2.py tests\unit\test_core_components.py -q -p no:cacheprovider`
2. 完整测试通过：
   - `pytest tests\unit tests\integration -q -p no:cacheprovider`
   - 结果：`60 passed, 4 warnings in 1.63s`

问题与后续计划：
1. 当前已完成项目上下文感知推荐、命令参数模式抽取、`mem suggest` 前缀推荐基础能力
2. 后续继续推进：
   - 命令执行反馈闭环，将 exit code 和成功率纳入排序
   - 矛盾更新机制，处理“不对/更正/以后用”等覆盖类记忆
   - Shell completion 脚本，接入 PowerShell/Bash/Zsh 的真实补全体验

---

【开发日志】时间：2026-04-28 10:45

本次新增：
1. 完成 CLI 命令执行反馈闭环
2. `cli/main.py` 新增 `_record_command_usage()`，用于执行后回写命令结果
3. `backend/routers/cli.py` 新增执行结果统计：
   - `success_count`
   - `failure_count`
   - `last_exit_code`
   - `last_failed_at`

本次修改：
1. `mem search --execute` 执行命令后自动上报 `exit_code`
2. `mem search` 交互模式中选择执行后，也会上报执行结果
3. `mem workflow run` 每个确认执行的 step 执行后，都会上报该 step 的执行结果
4. `/cli/command/record` 在收到 `exit_code` 时累计成功/失败次数
5. `/cli/command/suggest` 排序加入成功率信号
6. `core/retriever.calculate_cli_relevance_score()` 加入成功率得分，使向量检索排序也能受执行反馈影响

测试结果：
1. 针对性测试通过：
   - `pytest tests\unit\test_cli_phase2.py tests\unit\test_backend_cli_api.py tests\unit\test_core_components.py -q -p no:cacheprovider`
   - 结果：`38 passed, 4 warnings`
2. 完整测试通过：
   - `pytest tests\unit tests\integration -q -p no:cacheprovider`
   - 结果：`63 passed, 4 warnings in 5.69s`

问题与后续计划：
1. 当前推荐排序已经综合频率、最近使用、目录上下文、命令模式和执行成功率
2. 下一步建议推进矛盾更新机制，处理“不对/更正/以后用”等覆盖类记忆

---

【开发日志】时间：2026-04-28 11:10

本次新增：
1. 完成矛盾更新机制
2. `backend/routers/memory.py` 新增纠正意图识别：
   - `不对`
   - `更正`
   - `改成`
   - `以后用`
   - `以后改`
   - `不再使用`
   - `更新`
3. 新增 `topic_key`、`status`、`supersedes`、`superseded_by` 等记忆覆盖元数据
4. 新增中文短语检索增强，支持“发送周报”命中“周报发给”

本次修改：
1. `store_memory()` 写入纠正类记忆时，会查找同主题旧记忆并标记为 `inactive`
2. 新记忆会记录其覆盖的旧记忆 ID
3. `_search_temp()` 和 `_search_db()` 检索时过滤 `metadata.status == inactive` 的旧记忆
4. 向量检索结果转为关系库记录后，也会过滤 inactive 旧记忆

测试结果：
1. 针对性测试通过：
   - `pytest tests\unit\test_memory_and_feishu_routes.py -q -p no:cacheprovider`
   - 结果：`13 passed, 1 warning`
2. 完整测试通过：
   - `pytest tests\unit tests\integration -q -p no:cacheprovider`
   - 结果：`64 passed, 4 warnings in 5.85s`

问题与后续计划：
1. 当前已支持“以后周报发给 A -> 不对，以后周报发给 B”的覆盖语义
2. 后续可继续推进 shell completion 脚本和自动化评测脚本

---

【开发日志】时间：2026-04-28 11:35

本次新增：
1. 新增 CLI 方向自动化评测测试：
   - `tests/evaluation/test_cli_effectiveness_dataset.py`
2. 将 `test/data/cli_effectiveness_dataset.json` 接入 pytest

本次修改：
1. 抗干扰测试自动化：
   - 注入 6 条关键 CLI 记忆
   - 注入 360 条干扰数据
   - 自动计算并断言 `Hit@1`、`Hit@3`、`MRR`
2. 矛盾更新测试自动化：
   - 注入旧记忆和新记忆
   - 验证新记忆 Top1 胜出
   - 验证旧记忆被标记为 `inactive`
3. 效能指标测试自动化：
   - 复算字符节省率
   - 复算操作步数节省率
   - 复算耗时节省率
   - 与数据集 summary metrics 对齐

测试结果：
1. 评测测试通过：
   - `pytest tests\evaluation -q -p no:cacheprovider`
   - 结果：`3 passed, 1 warning in 0.82s`
2. 全量测试通过：
   - `pytest tests -q -p no:cacheprovider`
   - 结果：`67 passed, 4 warnings in 5.76s`

问题与后续计划：
1. 当前比赛要求中的抗干扰、矛盾更新、效能指标验证已经具备自动化测试入口
2. 后续可基于该 evaluation 测试输出生成答辩报告，或继续接入 Shell completion

---

【开发日志】时间：2026-04-29

本次新增：
1. 完成方向B“飞书项目决策与上下文记忆”的核心开发。
2. 新增飞书协同记忆路由：
   - `POST /api/v1/feishu/decision/extract`
   - `POST /api/v1/feishu/memory/query`
   - `POST /api/v1/feishu/message/analyze`
   - `POST /api/v1/feishu/event/callback`
3. 完成飞书消息中的决策语义识别与结构化抽取：
   - 项目主体：`project`
   - 决策主题：`topic`
   - 落地结论：`conclusion`
   - 原因：`reason`
   - 推荐参数：`preferred_terms`
   - 废弃参数：`rejected_terms`
   - 覆盖键：`topic_key`
4. 飞书决策会写入统一 `memories` 表，真实数据库模式下同步写入 `decision_memories` 表。
5. 飞书查询侧新增卡片化响应，支持返回历史 CLI 命令卡片、工作流卡片和决策卡片。
6. CLI `suggest` 新增团队决策干预排序：命中飞书最新决策推荐参数的命令上浮，命中废弃参数的命令下沉。

本次修改：
1. `backend/main.py` 挂载飞书路由，使方向B接口进入后端主服务。
2. `backend/routers/feishu.py` 从原签名校验骨架升级为飞书消息分析、决策入库、跨库查询的核心入口。
3. `backend/routers/cli.py` 的命令推荐排序加入 `project_decision` 活跃记忆，形成“飞书团队决策 -> CLI 推荐重排”的闭环。
4. 文档同步更新：
   - `开发文档/bot/1-飞书查询卡片与跨库联动.md`
   - `开发文档/bot/2-记忆提取与决策入库.md`
   - `开发文档/bot/3-检索关联与主动推送.md`
   - `开发文档/bot/4-决策干预与CLI动态重排.md`

测试结果：
1. 针对性测试通过：
   - `pytest tests\unit\test_memory_and_feishu_routes.py tests\unit\test_backend_cli_api.py -q -p no:cacheprovider`
   - 结果：`26 passed, 3 warnings in 1.05s`
2. 全量测试通过：
   - `pytest -q -p no:cacheprovider`
   - 结果：`70 passed, 4 warnings in 3.90s`

问题与后续计划：
1. 当前飞书侧已经具备方向B核心闭环：消息识别、决策入库、主动查询、CLI 推荐干预。
2. 后续可继续接入真实飞书 OpenAPI 的消息发送能力，把当前返回的卡片 JSON 渲染为飞书交互卡片。
3. 后续可将规则抽取升级为 LLM 抽取，在复杂多人讨论中提取“决策-理由-反对意见-结论”的完整结构。

---

【开发日志】时间：2026-04-29 larksuite/cli 接入

本次新增：
1. 参考官方 `larksuite/cli` 仓库，将飞书消息发送层改为调用 `lark-cli`。
2. 新增 `feishu_bot/lark_cli.py`，封装：
   - `lark-cli im +messages-send --as bot --chat-id <chat_id> --text <text> --format json`
3. `POST /api/v1/feishu/message/push` 现在会通过 `lark-cli` 发送群文本消息。
4. `POST /api/v1/feishu/message/analyze` 支持自动回复：
   - 设置 `FEISHU_AUTO_REPLY=true` 后，查询命中会把 CLI/工作流/决策卡片转为文本发回群。
   - 决策入库成功后，会发回“已记录团队决策”的确认消息。

本次修改：
1. `backend/routers/feishu.py` 新增 `_cards_to_text()`、`_send_group_text()`、`_auto_reply_enabled()`。
2. 自动回复默认关闭，避免本地测试或未安装 `lark-cli` 时误触发外部发送。
3. 当未安装或未认证 `lark-cli` 时，发送层返回结构化错误，不影响决策入库和数据库更新。

测试结果：
1. 飞书方向单测通过：
   - `pytest tests\unit\test_memory_and_feishu_routes.py -q -p no:cacheprovider`
   - 结果：`18 passed, 1 warning in 0.96s`
2. 全量测试通过：
   - `pytest -q -p no:cacheprovider`
   - 结果：`72 passed, 4 warnings in 3.83s`

真实接入前置命令：
```bash
lark-cli config init
lark-cli auth login
```

环境变量：
```env
FEISHU_AUTO_REPLY=true
LARK_CLI_BIN=lark-cli
LARK_CLI_TIMEOUT_SECONDS=15
```

---

【开发日志】时间：2026-04-29 飞书 SDK 事件订阅接入

本次新增：
1. 新增 `feishu_bot/sdk_events.py`，使用飞书服务端 SDK 的事件分发器和长连接客户端接收群消息。
2. 新增 `scripts/run_feishu_sdk_events.py`，用于启动 SDK 事件订阅进程。
3. SDK 收到 `P2ImMessageReceiveV1` 消息事件后，会转换为项目内部统一消息结构：
   - `content`
   - `chat_id`
   - `message_id`
   - `user_id`
   - `mentioned`
   - `source=feishu_group`
4. 转换后的消息会请求 `POST /api/v1/feishu/message/analyze`，复用现有方向B核心逻辑。

本次修改：
1. 事件订阅从“只能 HTTP 回调”扩展为“HTTP 回调 + SDK 长连接”双模式。
2. SDK 模块采用懒加载方式导入 `lark_oapi`，未安装 SDK 时不影响普通后端启动和单元测试。
3. 群消息经 SDK 进入后端后，仍然支持：
   - 决策消息写入真实数据库。
   - 查询消息检索 CLI/工作流/决策记忆。
   - 设置 `FEISHU_AUTO_REPLY=true` 后自动回群。

测试结果：
1. 飞书方向单测通过：
   - `pytest tests\unit\test_memory_and_feishu_routes.py -q -p no:cacheprovider`
   - 结果：`20 passed, 1 warning in 1.36s`
2. 全量测试通过：
   - `pytest -q -p no:cacheprovider`
   - 结果：`74 passed, 4 warnings in 3.79s`

真实运行方式：
```bash
uvicorn backend.main:app --host 127.0.0.1 --port 8000
python scripts/run_feishu_sdk_events.py
```
